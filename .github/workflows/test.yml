name: Test
on: workflow_dispatch

jobs:
  hello_world_job:
    runs-on: ubuntu-latest
    name: A job to say hello
    steps:
      - uses: actions/checkout@v4
      - name: Set outputs
        id: vars
        shell: bash
        run: echo "::set-output random-number=number::five"

      - run: echo random-number ${{ steps.foo.outputs.random-number }}
        shell: bash