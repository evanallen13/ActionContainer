name: "Issue Close"
on:
  workflow_dispatch:

env:
    GH_TOKEN: ${{ github.token }}
    GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
    OWNER: ${{ github.repository_owner }}
    REPO: ${{ github.event.repository.name }}

jobs:
    closeBugs:
        name: Close all issues labeled "bug"
        runs-on: ubuntu-latest
        steps:
            # - name: Find all bugs
            #   id: bugs
            #   uses: lee-dohm/select-matching-issues@v1
            #   with:
            #     query: 'label:bug'
            #     token: ${{ secrets.PAT_TOKEN }}

            # - name: Close found issues
            #   run: cat ${{ steps.bugs.outputs.path }}

            # - name: List all issues
            #   run: |
            #     echo "List all issues"
            #     gh issue list 
            
            - name: Get Open Issues
              run: |
                repo_owner="evanallen13"
                repo_name="ActionContainer"

                gh issue list -R="$repo_owner/$repo_name" -s=open\ -l bug


